prefix=@DEST_DIR@
exec_prefix=${prefix}
libdir=${prefix}/lib
includedir=${prefix}/include

Name: warabi-admin
Description: Blob storage component for Mochi
Version: @WARABI_VERSION@

Requires: thallium uuid nlohmann_json
Libs: -L${libdir} @ADMIN_PRIVATE_LIBS@ -lstdc++
Cflags: -I${includedir}
